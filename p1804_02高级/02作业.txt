

练习：
只读方式打开一个文件，并关闭
写入方式打开文件，并关闭
追加方式打开一个文件，并关闭

--------- 练习
利用open 创建一个文件，并写入一首唐诗
读取每一行的内容，并打印，在每行结尾 添加  *-* 符号
将上述显示的内容，重新 追加到 文件的 后边 ，每段用  |  分割链接

练习2：
写入文件唐诗一首；
使用 readlines，读取多有行；
遍历 读取到的列表  ，并给没条数据后边添加 *号
打印输出；

---------------练习  （tell,seek）
写入文件唐诗
读取每一行 ，  并 打印读取后的 游标位置
移动游标位置，重复读取 第一行 前5个内容
循环遍历 唐诗文件，逐一打印游标的位置变化 数量
1. 定义一个函数，型参是文件名称
2. 函数功能实现，型参对应的文件，存在直接打开，并显示内容；
3. 如果文件不存在，或者内容为空，则给文件内添加 ‘哈哈哈’  并打印；
4. 关闭文件    调用函数演示；

练习：  os   rename  remove
定义一个删除函数；型参是要删除的文件名
函数功能，删除给定的文件名对应的文件
定义一个改名函数；型参是 源文件名，新文件名
功能： 实现文件改名，并打印输出新的文件名

----------练习
创建一个函数，名称和型参自定；
函数功能：调用函数，传递一个路径，如果存在则设定 为默认路径
如果不存在则创建 文件目录，并设定为默认路径

创建函数，删除指定 文件夹，并调用
创建函数，在指定路径，新建指定文件名的方法



